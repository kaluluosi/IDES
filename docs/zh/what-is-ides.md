# 什么是 IDES

IDES 是一个 RUST 原生记忆 AI 智能体底座。它最想传递给你的一件事实是：**别人要折腾的那些，IDES 都不用你碰。**

IDES 的设计哲学，围绕两根支柱展开——**开箱即用（精装房）** 和 **数据全本地（本地非云端）**。

## 设计哲学

### 开箱即用：精装房，连部署都没有

很多 agent 底座入手要先过几道坎：命令行走一遍装运行时、配一堆依赖、本地编译、再起服务调环境。还没开始干正事，人先累趴了。

IDES 不是这样。它像一套**精装房**——拿到就能住：

| | 其他底座 | IDES |
|------|------|------|
| 安装 | 命令行、装依赖、编译 | 一个安装包，双击即装 |
| 部署 | 起服务、配环境 | 没有「部署」这回事 |
| 配置 | 一堆文件 / 环境变量 | 默认就够，很少要改 |
| 硬件门槛 | 吃配置、挑机器 | 老电脑、5G 内存也能流畅跑 |

这套「精装房」不是形容词，是能验证的事实：

- **纯 Rust 原生，零运行时依赖。** 不折腾依赖、不纠结版本、不陷在配置里。
- **可选便携模式。** 解压即用、绿色无痕，能装进 U 盘随身带。
- **老机器也能跑。** 实测在 2015 年的 HP 笔记本、5G 内存上跑得流畅——不逼你换电脑。

!!! note "为什么配置项这么少？"
    因为 IDES 把该做好的都做好了。对话循环、意图识别、工具调用、系统提示、上下文管理全部内建——你不用跟 prompt 搏斗，不用自己搭上下文、管记忆。**配置项少，不是内容单薄，是产品已经替你优化到「不需要配置」。**

!!! quote "工欲善其事，必先利其器。"
    器已利，事已备，只待开始。
    <span class="src">—— 《论语》</span>

这套「精装房」，是 **自研 AGENT 管线**的最优解——**无需自己折腾 agent loop / prompt / 上下文管理**。

### 数据全本地：本地非云端

很多 AI 服务把你的数据往云端送。IDES 反着来——**所有数据都在本地，零云端。**

- **记忆本地加密储存**，不上传、不外泄，还你**记忆主权**。
- **换机、迁移、重装**，把记忆带走，这个 AI 还是「同一个我」。
- 数据不离你的手，你始终知道它在哪、属于谁。

!!! note "记忆是你的，不是平台的"
    有的 agent 记忆要靠外部服务（向量库、图库、LLM）拼装，容易**串用户、越用越脏、还会悄悄腐烂**。IDES 的记忆是**内生的、本地的**——干净、不串、不丢。

### 记忆：始终记得同一个你

IDES 没有「新会话 / 旧会话」的概念——它始终是**同一个 agent**。你知道它，它认得你；重启、换机，都是同一个「我」。

这是 IDES 的核心所在，我们单独用一整章讲它 → [GNM 记忆详解](gnm.md)。

## 开发团队

IDES 由 [待补充——团队介绍] 开发。

> 我们相信：工具应该为人服务，而不是反过来折腾人。所以 IDES 把该做的都做好，把你从配置、依赖、编译里解放出来——把那点心思，留给真正重要的事。
